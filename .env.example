# Copy this file to ".env" and fill it in — OR just use the app (Jarvis.bat),
# which writes this file for you when you click Save. ".env" is gitignored.

# Which provider to use: openrouter | zai | deepseek | qwen
JARVIS_PROVIDER=openrouter

# Paste the key for whichever provider you chose above.
# OpenRouter is free and recommended (one key reaches Qwen3 + GLM + more).
OPENROUTER_API_KEY=      # https://openrouter.ai/keys   (FREE)
ZAI_API_KEY=             # https://z.ai/manage-apikey/apikey-list   (GLM)
DEEPSEEK_API_KEY=        # https://platform.deepseek.com/api_keys
QWEN_API_KEY=            # https://bailian.console.alibabacloud.com/

# Optional overrides
# JARVIS_MODELS=glm-4.6            # force a specific model (blank = provider default)
# JARVIS_BASE_URL=                # custom OpenAI-compatible endpoint
# JARVIS_SPEAK=1                   # 0 = don't speak replies out loud
# JARVIS_TTS_RATE=180
# JARVIS_STT_LANG=en-US
# JARVIS_CLAP_THRESHOLD=0.22       # lower = more sensitive claps
# JARVIS_BLOCK_DANGEROUS=1         # 0 = allow destructive shell commands (max power)
# JARVIS_WORKSPACE=                # where Jarvis writes generated files
